targets:
  $default:
    builders:
      drift_dev:
        options:
          # ISO-8601 text keeps millisecond precision and sorts correctly.
          store_date_time_values_as_text: true
          named_parameters: true
