# design/

Reference HTML mockups — **read-only**. Rebuild in Flutter, never embed.

- `patient_*.html`: visual source of truth (colors, glass, type, radii, spacing).
- `pharmacy_*_layout.html`, `admin_*_layout.html`: layout source of truth only (old light palette — rebuild with dark tokens).
- `[bracketed]` text is placeholder data.
